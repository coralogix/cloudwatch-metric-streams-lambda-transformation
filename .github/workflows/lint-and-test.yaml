name: lint-and-test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Linters for Go code
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.26.x

      - name: Lint
        uses: golangci/golangci-lint-action@v7
        with:
          # v2.5.0+ required for Go 1.26 (official binaries built with go1.26).
          version: v2.5
          args: --timeout=5m

  test:
    runs-on: ubuntu-latest
    name: Runs unit tests and test build
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.26.x

      - name: Run tests
        run: make test

      - name: Test build
        run: make build
